/**
 * 자바스크립트 문자열 
 *
 * - ""로 감싼다.
 * - ''로 감싼다.
 * - ``(backtick)으로 감싼다.
 */

let name = "홍길동";
let age = 33;

// let msg = "이름은 "+name+", 나이는"+age+"입니다.";
let msg2 = `이름은 ${name}, 나이는 ${age} 입니다.`;
console.log(msg);
console.log(msg2);
